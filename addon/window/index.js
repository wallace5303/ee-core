/**
 * 窗口模块
 */
module.exports = (app) => {
  const window = {

    /**
     * create window
     *
     * @function 
     * @since 1.0.0
     */
    createWindow: () => {
      return 'ddd';
    }


  };

  return window;
}
